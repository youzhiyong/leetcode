1. 类的实例化顺序，比如父类静态数据，构造函数，字段，子类静态数据，构造函数，字段，当new的时候，他们的执行顺序。

初始化顺序:
    基类静态字段、静态代码块(并列优先级，顺序和代码出现的顺序一致)
    ---->派生类静态字段、静态代码块(顺序同基类)
    ---->基类普通字段、普通代码块(顺序同代码中出现的顺序)
    ---->基类构造方法
    ---->派生类普通字段、普通代码块(顺序同基类)
    ---->派生类构造方法